package fitnesse.idea.decisiontable

import fitnesse.idea.fixturemethod.MethodOrScenarioArgumentReference

class DecisionOutputReference(referer: DecisionOutput) extends MethodOrScenarioArgumentReference(referer) {

  // Handle rename??
  // override def handleElementRename
}
